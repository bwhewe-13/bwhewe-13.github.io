# RL with Python — pip requirements
# Usage: pip install -r requirements.txt
#
# NOTE: PyTorch is listed here as the CPU build.
# For GPU support, install manually first:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# then run: pip install -r requirements.txt

# Core deep learning
torch>=2.3.0
torchvision
torchaudio

# RL environments
gymnasium>=1.0.0
ale-py>=0.10.0
pygame>=2.6.0
pettingzoo>=1.25.0

# RL frameworks
stable-baselines3>=2.7.0
sb3-contrib>=2.7.0
ray[rllib]>=2.54.0
mlflow>=2.14.0

# Wrappers & compatibility
supersuit>=3.9.0
shimmy>=1.3.0

# Utilities
numpy>=1.26.0
matplotlib>=3.9.0
tensorboard>=2.17.0
